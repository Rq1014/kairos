<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.kairos.mapper.dict.GradSchoolMapper">

    <resultMap id="GradMap" type="org.example.kairos.entity.GradSchoolEntity">
        <id column="id" property="id"/>
        <result column="university_id" property="universityId"/>
        <result column="code" property="code"/>
        <result column="name_cn" property="nameCn"/>
        <result column="name_jp" property="nameJp"/>
        <result column="name_en" property="nameEn"/>
        <result column="category" property="category"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findByUniversityId" resultMap="GradMap">
        SELECT * FROM grad_school
        WHERE university_id = #{universityId} AND status = 1
        ORDER BY sort_order DESC, id ASC
    </select>

    <select id="findAll" resultMap="GradMap">
        SELECT * FROM grad_school WHERE status = 1 ORDER BY university_id, sort_order DESC, id ASC
    </select>

    <select id="findByUniversityIdAndCode" resultMap="GradMap">
        SELECT * FROM grad_school
        WHERE university_id = #{universityId} AND code = #{code} AND status = 1
    </select>

    <select id="findByUniversityIdAndNameJp" resultMap="GradMap">
        SELECT * FROM grad_school
        WHERE university_id = #{universityId} AND name_jp = #{nameJp} AND status = 1
        LIMIT 1
    </select>
</mapper>
